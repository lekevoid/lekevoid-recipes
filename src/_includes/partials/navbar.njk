<div class="flex w-full lg:w-48">
    <nav
        class="flex items-center justify-between lg:justify-between flex-wrap p-6 lg:px-0 container mx-auto" x-data="{ isOpen: false }" @keydown.escape="isOpen = false" @click.away="isOpen = false">
        <!--Logo etc-->
        <div class="flex items-center lg:fixed lg:top-0 lg:left-0">
            <a href="/" class="border-white">
                <img src="/static/img/coeus_chef.png" height="50" width="50" style="object-fit:contain; height:50px; width:50px;" class="no-border"/>
            </a>
        </div>
        <!--Toggle button (hidden on large screens)-->
        <div class="lg:hidden">
            <button @click="isOpen = !isOpen" type="button" class="ml-auto block lg:hidden px-2 text-primary-500 hover:text-primary-500 focus:outline-none focus:text-primary-500" :class="{ 'transition transform-180': isOpen }" aria-label="Menu">
                <svg class="h-6 w-6 fill-current" xmlns="http://www.w3.org/2000/svg" viewbox="0 0 24 24">
                    <path x-show="isOpen" fill-rule="evenodd" clip-rule="evenodd" d="M18.278 16.864a1 1 0 0 1-1.414 1.414l-4.829-4.828-4.828 4.828a1 1 0 0 1-1.414-1.414l4.828-4.829-4.828-4.828a1 1 0 0 1 1.414-1.414l4.829 4.828 4.828-4.828a1 1 0 1 1 1.414 1.414l-4.828 4.829 4.828 4.828z"/>
                    <path x-show="!isOpen" fill-rule="evenodd" d="M4 5h16a1 1 0 0 1 0 2H4a1 1 0 1 1 0-2zm0 6h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2zm0 6h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2z"/>
                </svg>
            </button>
        </div>
        <!--Menu-->
        <div class="w-full flex-grow lg:flex lg:flex-col lg:items-center lg:w-auto text-center lg:fixed lg:top-12 lg:left-0" :class="{ 'block shadow-3xl': isOpen, 'hidden': !isOpen }" x-show.transition="true">
            {% if recipes_categories.items %}
                <div class="flex flex-col m-0">
                    {% for item in recipes_categories.items %}
                        <div class="nav__item mb-3 m-0 flex flex-row flex-nowrap items-center">
                            <input type="checkbox" name="category_{{ item.slug }}" value="{{ item.slug }}" x-model="filterCategories">
                                <label for="category_{{ item.slug }}" class="ml-1">{{ item.name }}</label>
                            </div>
                        {% endfor %}
                    </div>
                {% endif %}
            </div>
        </nav>
    </div>